const routes = {
    // Client
    home: "/",
    login: "/login",
    register: "/register",

    // Admin
    loginAdmin: "/admin",
    dashboard: "/admin/dashboard",
    users: "/admin/users",
    roles: "/admin/roles",
    permissions: "/admin/permissions",
    settings: "/admin/settings",
};

export default routes;
